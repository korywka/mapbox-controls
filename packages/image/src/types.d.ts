export type { IControl } from "mapbox-gl";

export interface ControlOptions {
	removeButton?: boolean;
}

export type RasterCoordinates = [[number, number], [number, number], [number, number], [number, number]];
